def fun()={

  var  k,i,j = 2 
  val m,n = 5

  val f = 12.0f 
  val g = 4.0f
  val c = 'X'

  println("\n(a) = " + (k+12*m))
  println("(b) = " + m/j)
  println("(c) = " + n%j)
  println("(d) = " + m/j*j)
  println("(e) = " + (f+10*5+g))

  i = i + 1

  println("(f) = " + (i*n))

  println("\nIn Java, we can use postfix and prefix operaters. But in Scala we can't use  prefix and postfix operaters.\n");

}

fun()
